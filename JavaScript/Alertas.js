"use strict"

    alert( "ALERTA!!!!!" );

    var resultado = confirm( "Estas seguro de continuar" );
    console.log( resultado );

    var edad = prompt( "Que edad tienes?", ); 
    console.log( edad );