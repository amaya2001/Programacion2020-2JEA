"use strict"

    //Operaciones
    var num1 = 7, num2 = 12, operacion  = 0;

    operacion = num1 + num2;
    alert( "El resultado es: " + operacion );

    var numeroEntero = 44;
    var texto_cadena = "Hola parce";
    var booleano = true;

    var numeroFalso = "33";
    console.log( parseInt (numeroFalso ) + 7 );
    console.log( typeof numeroEntero );
    console.log( typeof texto_cadena );
    console.log( typeof booleano );

    //Podemos ver que pasa en un bucle debugger;
    