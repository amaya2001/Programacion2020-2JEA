#include<bits/stdc++.h>
using namespace std;

vector< vector< int > > adj( 100 );
vector< bool > visitados( 100 );

int main(){

    int w, h;
    char value[ 100 ][ 100 ];
    while( scanf(" %d %d", &w, &h ) != EOF ){
        for( int i = 0; i < h; ++i ){
            for( int j = 0; j < w; ++j ){
               cin>> value[ i ][ j ]; 
            }

        }
    }
}

